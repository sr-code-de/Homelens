# Homelens
Homelens auto-discovers your Proxmox nodes, VMs, LXCs, and Docker containers and renders them as an interactive service map. See your entire stack in one view — what runs where, what depends on what, and what's currently healthy.
